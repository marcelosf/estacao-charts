
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-01-01' AND '2020-01-16'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-01-01' AND '2020-01-24'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-01-01' AND '2020-01-24'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin,

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin,

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        1SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        1SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
                date_format(data, '%Y-%m-%d') data_formatada,
                AVG(temp_bar) temp_bar,
                AVG(pressao) pressao,
                AVG(tseco) tseco, 
                AVG(tumido) tumido,
                MAX(tseco) tmax,
                MIN(tseco) tmin
            
            FROM dados 
            
            WHERE data BETWEEN '2021-01-19' AND '2021-01-20'
            
            GROUP BY data_formatada;
            
            SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        SELECT 
            data data_formatada,
            temp_bar,
            pressao,
            tseco,
            tumido,
            tmax,
            tmin

            FROM dados

            WHERE data BETWEEN '2020-02-28' AND '2020-02-29'
        